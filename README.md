# ss-# 🔒 資安 Landing Page 設計計畫

## 設計方向：iOS-Inspired Glassmorphism Security

以 Apple / iOS 設計語言為核心，打造一個充滿信任感與科技感的資安產品 Landing Page。

-----

## 🎨 視覺風格

### 核心美學

- **iOS 圓角系統**：所有卡片、按鈕、容器皆採用 `border-radius: 20px~28px` 的大圓角
- **Glassmorphism（毛玻璃）**：半透明背景 + `backdrop-filter: blur()` 模擬 iOS 控制中心質感
- **深色主題**：深藍黑底色，傳達專業、安全的資安氛圍
- **微妙漸層**：卡片邊框使用 1px 半透明漸層光暈，模擬 iOS 玻璃邊緣反射

### 色彩系統

|用途  |色值                              |說明            |
|----|--------------------------------|--------------|
|背景  |`#0a0e1a` → `#111827`           |深空黑藍漸層        |
|主色  |`#3b82f6` → `#06b6d4`           |藍到青的安全感漸層     |
|強調  |`#22d3ee`                       |霓虹青，用於 CTA 和重點|
|警告  |`#f59e0b`                       |琥珀色，用於威脅指標    |
|成功  |`#10b981`                       |翡翠綠，用於安全狀態    |
|卡片背景|`rgba(255, 255, 255, 0.05~0.08)`|半透明白          |
|卡片邊框|`rgba(255, 255, 255, 0.1)`      |微光邊緣          |

### 字型

- **標題**：`SF Pro Display`（fallback: `-apple-system, system-ui`）
- **內文**：`SF Pro Text`（fallback: `'Noto Sans TC', sans-serif`）
- 中文搭配 Google Fonts `Noto Sans TC`

-----

## 📐 頁面結構

### Section 1 — Hero

- 全屏漸層背景 + 浮動的抽象粒子/網格動畫（純 CSS 或輕量 JS）
- 大標題：「守護你的數位世界」
- 副標題簡述產品價值
- 兩個 iOS 風格圓角按鈕（主 CTA：開始免費試用 / 次 CTA：了解更多）
- 中央放置一個 3D 感的盾牌 icon 或 dashboard mockup

### Section 2 — 威脅概覽（數據面板）

- 模擬 iOS Widget 的圓角卡片排列（2×2 或 3 列）
- 每張卡片顯示一個資安指標：
  - 🛡️ 已攔截威脅數量（動態計數動畫）
  - ⚡ 平均回應時間
  - 🌐 監控端點數
  - ✅ 系統可用率 99.99%
- 卡片使用 glassmorphism 效果

### Section 3 — 核心功能（Feature Cards）

- 3~4 張大型圓角功能卡片，水平滾動或網格排列
- 每張卡含：
  - 漸層 icon 圓形背景
  - 功能名稱
  - 一段簡述
- 功能項目：
1. **即時威脅偵測** — AI 驅動的異常行為分析
1. **端點防護** — 全面裝置管理與保護
1. **零信任架構** — 最小權限、持續驗證
1. **合規報告** — 一鍵產出 ISO 27001 / SOC2 報告

### Section 4 — 防護流程（How It Works）

- 水平時間軸或垂直步驟流程
- 3 個圓角步驟卡片，以連接線串聯
1. 部署 Agent → 2. 智能分析 → 3. 自動回應
- 每步配有簡單動畫 icon

### Section 5 — 客戶信任（Social Proof）

- iOS 風格的橫向滑動 testimonial 卡片
- 企業 logo 列（灰階 → hover 顯色）
- 大型數字指標：「保護超過 1,000+ 企業」

### Section 6 — 定價方案

- 3 張圓角定價卡片（Basic / Pro / Enterprise）
- 推薦方案加上漸層邊框高亮
- 每張卡含功能清單 + CTA 按鈕
- 切換月繳/年繳的 iOS Toggle 開關

### Section 7 — CTA + Footer

- 大型 CTA 區塊：深色毛玻璃背景 + 漸層按鈕
- Footer：簡潔的連結列 + 版權資訊

-----

## ✨ 互動與動畫

|元素       |動畫效果                                     |
|---------|-----------------------------------------|
|卡片進場     |`IntersectionObserver` 觸發 fade-up + scale|
|數字指標     |countUp 動畫（滾入視窗時啟動）                      |
|按鈕 hover |微縮放 `scale(1.02)` + 光暈擴散                 |
|卡片 hover |微抬升 `translateY(-4px)` + 陰影加深            |
|背景       |緩慢移動的漸層光點（CSS `@keyframes`）              |
|步驟連接線    |滾動時漸進描繪（stroke-dashoffset 動畫）            |
|Toggle 開關|彈性滑動（cubic-bezier 過渡）                    |

-----

## 🧩 元件設計規範

### 圓角尺寸系統（iOS 風格）

```
--radius-sm: 12px;   /* 小按鈕、tag */
--radius-md: 16px;   /* 輸入框、小卡片 */
--radius-lg: 20px;   /* 功能卡片 */
--radius-xl: 28px;   /* 大型區塊、hero 元素 */
--radius-full: 9999px; /* 膠囊按鈕、頭像 */
```

### 毛玻璃效果模板

```css
.glass-card {
  background: rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-lg);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}
```

### 按鈕樣式

```css
.btn-primary {
  background: linear-gradient(135deg, #3b82f6, #06b6d4);
  border-radius: var(--radius-full);
  padding: 14px 32px;
  font-weight: 600;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
```

-----

## 📁 技術實作

- **格式**：單一 HTML 文件（內嵌 CSS + JS）
- **框架**：Pure HTML/CSS/JS（或 React JSX）
- **字型載入**：Google Fonts CDN（Noto Sans TC）
- **圖標**：Lucide Icons 或內嵌 SVG
- **動畫**：CSS animations + IntersectionObserver
- **RWD**：Mobile-first，斷點 768px / 1024px

-----

## 📝 下一步

1. ✅ 確認設計計畫（本文件）
1. ⬜ 實作 HTML Landing Page
1. ⬜ 微調動畫與互動細節
1. ⬜ RWD 測試與優化

-----

> 💡 **設計理念**：將 iOS 的圓潤、毛玻璃、精緻動效語言融入資安產品，
> 打破傳統資安網站「冰冷、技術化」的刻板印象，
> 以親和力建立信任感。
